import { UserTag } from "../../models";
import { UserModelInterface } from "../../models/user";

const userTag = {
  create(user: UserModelInterface) { },

  remove(id: string) { },
};

export default userTag;
