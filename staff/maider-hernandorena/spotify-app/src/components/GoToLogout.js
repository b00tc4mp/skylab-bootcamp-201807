import React from 'react'

function GoToLogout(props) {
    return <section>
                <button onClick={() => props.clickItem(id)}>Logout</button>
            </section>
}

export default GoToLogout