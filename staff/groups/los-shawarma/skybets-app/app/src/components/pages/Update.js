import React from 'react';
import { Link } from 'react-router-dom';

const Update = () => (
    <section>
        <h1>Update Profile</h1>
        <Link to="/">Home</Link>
    </section>
);

export default Update;