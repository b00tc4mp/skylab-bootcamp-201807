import React from 'react';
import { Link } from 'react-router-dom';

const Profile = () => (
    <section>
        <h1>Profile</h1>
        <Link to="/">Home</Link>
    </section>
);

export default Profile;